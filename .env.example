# DayLoop Configuration
PORT=3001
# Set to 'production' to serve built frontend
NODE_ENV=development
# Database path (default: backend/data/dayloop.db)
# DB_PATH=backend/data/dayloop.db
# Agent Integration API 服务间认证 Token（§43，Agent Service 需配相同值）
# DAYLOOP_SERVICE_TOKEN=change-me-service-token
